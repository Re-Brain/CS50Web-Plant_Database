from django import forms
from django.forms import ModelForm
from .models import plant

# Create forms heare
# class plantFormTop(ModelForm):
#     class Meta:
#         model = plant
#         fields = ('name', 'scientificName', 'uses', 'characteristic', 'distribution', 'habitat', 'care', 'location', 'references')

# class plantFormBotton(ModelForm):
#     class Meta:
#         model = plant